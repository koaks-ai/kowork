# KoWork Agent 工作指南

本文件是整个仓库的统一入口，包含两套规则：

- **重构规范**：说明当前实现、目标架构、迁移进度，以及进行重构前必须阅读的文档和阶段边界。
- **开发规范**：约束所有新功能、Bug 修复、重构和文档修改的实现质量、决策方式与验证要求。

所有任务都必须遵守“开发规范”。涉及目标架构、迁移计划、阶段任务或新旧实现切换时，还必须遵守
“重构规范”。无法确定任务是否属于重构时，按两套规范同时执行。

本文件适用于整个仓库。子目录如有更具体的 `AGENTS.md`，则在其作用域内补充或覆盖本文件。

## 一、重构规范

### 当前实现与重构目标

KoWork 当前仍是本地优先的 Electron 桌面编码 Agent。正在运行的主链路是：

```text
Renderer -> Preload -> Electron Main -> Core utilityProcess
```

当前由 TypeScript Core 负责项目访问、运行队列、持久化、权限审批和 Koaks Runtime；Koaks 通过
Kotlin/JS 与 Node.js 交互层导出的 NPM 包接入。

项目正在重构为支持本地与远程同构部署的客户端—服务端架构：

```text
Electron Client -> KAP over WebSocket -> Kotlin/Native Agent Server
```

目标架构中，Electron 是纯客户端；工作目录访问、命令执行、模型调用、Agent 状态和持久化全部归
Agent Server。本地模式由 Electron 启动同一个 Server 二进制作为 sidecar，远程模式由用户部署该
二进制，两种模式必须共用相同的执行路径。

注意：[`docs/architecture.md`](docs/architecture.md) 描述的是目标架构，不代表其中所有目录和能力
都已落地。未完成阶段仍以当前代码、测试和迁移状态为准，不得把目标设计误当成现有实现。

### 当前迁移进度

当前阶段快照：

- 阶段 0：KAP v1 协议基线——已完成。
- 阶段 1：设计系统与交互原语统一——已完成。
- 阶段 2：主题体系——已完成。
- 阶段 3：Kotlin Agent Server——未开始；进入主体实现前必须先完成 Native 纵切 spike。
- 阶段 4：客户端换接 KAP 并删除旧 TS Core——未开始。
- 阶段 5：插件系统——未开始。
- 阶段 6：收尾、测试矩阵和多平台交付——未开始。

最新状态以 [`docs/architecture.md` 的“迁移状态”](docs/architecture.md#9-迁移状态) 和
[`docs/refactor-plan.md`](docs/refactor-plan.md) 为准。本节只保留便于导航的摘要，不作为独立的进度
真源。

### 开始重构任务前

1. 阅读 [`docs/architecture.md`](docs/architecture.md)，确认目标架构、进程边界、目录边界和最新迁移状态。
2. 在 [`docs/refactor-plan.md`](docs/refactor-plan.md) 中找到当前阶段，阅读该阶段的背景、范围、验收条件和“不做”事项。
3. 根据下表阅读与任务相关的专项规范和 ADR；不要无差别加载无关文档。
4. 检查对应的当前代码和测试。尚未完成的阶段仍使用旧实现，目标文档不能替代现状调查。
5. 实现应停留在当前任务和当前阶段的范围内；不要顺手启动后续阶段或提前删除当前阶段仍依赖的旧路径。

### 文档导航

| 文档 | 作用 | 何时必须阅读 |
| --- | --- | --- |
| [`docs/architecture.md`](docs/architecture.md) | 目标架构、进程边界、仓库规划、迁移状态和扩展原则 | 所有架构或重构任务 |
| [`docs/refactor-plan.md`](docs/refactor-plan.md) | 分阶段范围、顺序、技术前提、验收标准和非目标 | 开始或继续任一重构阶段 |
| [`docs/design-system.md`](docs/design-system.md) | UI token、组件、动画、主题、Inspector 和插件 UI 边界 | 界面、样式、动画、交互原语、主题、Inspector 或 UI 插件 |
| [`docs/protocol/kap-v1.md`](docs/protocol/kap-v1.md) | KAP 帧、方法、事件、错误、版本协商和一致性规则 | 协议、Agent Client、Agent Server 或跨进程通信 |
| [`ADR 0001`](docs/decisions/0001-agent-server-owns-execution.md) | Agent Server 拥有全部执行，客户端是纯展示层 | 工具、工作区、模型调用、执行位置或 Core/Server 边界 |
| [`ADR 0002`](docs/decisions/0002-server-owns-state.md) | Server 与客户端的状态归属 | 数据库、设置、凭据、权限、模型 Profile 或持久化 |
| [`ADR 0003`](docs/decisions/0003-dual-host-plugin-runtime.md) | UI 与 Agent 插件的双宿主方案及信任模型 | 插件 API、插件进程、Inspector 插件或插件热加载 |
| [`ADR 0004`](docs/decisions/0004-kotlin-native-hard-cutover.md) | Kotlin/Native 分发、前置 spike 和硬切换策略 | Agent Server、Native 构建、打包、切换或删除旧 Core |

如果实现需要推翻某条 ADR，不要静默绕过；先说明原决策不再成立的条件，并同步修改 ADR 和受影响的
架构、协议或重构计划。

### 阶段性代码边界

- `packages/core`、`packages/contracts` 和 `src/core` 仍属于当前可运行架构。阶段 4 完成前不能仅因其属于旧架构就提前删除；这是重构顺序约束，不是兼容性承诺。切换时按计划直接替换，禁止形成新旧两套长期并行实现。
- `agent/protocol` 是 KAP 的 Kotlin 真源，`packages/protocol` 是 TypeScript + Zod 镜像，`conformance/kap-v1-cases.json` 是两侧共享的一致性用例。修改协议必须同时维护这三处。
- `packages/design-system` 是通用视觉能力的唯一来源。Renderer 业务代码只消费其 token 和原语，不得建立第二套通用视觉实现。
- `agent/` 是独立 Gradle 构建。当前除协议基础外，大部分目标模块尚未实现；新增模块应遵循当前阶段的目录规划，不要通过占位实现伪造阶段完成。
- 阶段 3a 涉及仓库外的 Koaks 项目。除非任务明确把该仓库纳入范围，否则不要修改外部 Koaks 工作区。

### 阶段完成规则

- 完成一个重构阶段前，必须执行该阶段在 `docs/refactor-plan.md` 中列出的全部验收项。
- 阶段完成或架构假设发生变化时，必须同步更新迁移状态、实施结果、相关架构文档和受影响的 ADR。
- 不要声称未实现阶段已经完成，也不要用空壳、跳过测试或未记录的兼容层满足阶段验收。
- 当前阶段尚未达到验收条件时，应如实记录剩余工作和阻塞原因，不得通过缩小验收口径宣布完成。

## 二、开发规范

### 正确实现优先

- 开发新功能或修改现有模块时，先定位根因、职责边界和最合适的抽象，再按当前已知的最佳方案完整实现。目标是让实现自然地属于整体架构，而不是只让当前场景暂时工作。
- 除非当前场景适合, 否则尽量不要用局部特判、重复状态、绕过分层、临时适配器、隐式 fallback、复制实现或未记录的 feature flag 掩盖结构问题。不要留下“以后再整理”的补丁，因为后续代码通常会继续依赖它并形成补丁套补丁。
- 如果正确方案需要跨模块修改，就在任务允许的范围内完成完整闭环。如果所需改动会显著扩大任务范围，先向用户说明根因、完整方案和影响范围并请求决定，不得自行退而采用临时补丁。

### Bug 修复与失败尝试清理

- 修复 Bug 时应修复产生问题的根因，并补充能够防止回归的测试（除非是视觉改动这种不好写测试用例的）；不要只处理当前输入、当前页面或单条调用链上的表面症状。
- 如果一个 Bug 经历了多次尝试才最终修复，在确认最终方案有效后，应回溯检查本次 Bug 前几次尝试引入的全部修改。逐项判断它是否是最终根因修复、回归测试或必要重构的一部分：必要的保留，不必要的
  删除。尤其要清理失败尝试留下的临时判断、额外状态、日志、fallback、配置、依赖、错误抽象，以及
  只为旧尝试服务的测试；不得因为它们已经写完就继续保留。
- 清理范围只包括为本次 Bug 修复引入的修改，不得误删用户原有或其他任务的改动。若无法确定某项修改的归属或必要性，按“不确定决策必须询问”的规则请求用户确认。
- 清理完成后重新运行回归测试，确认在移除无关修改后 Bug 仍然得到修复。

### 允许破坏性变更

- 项目目前处于开发和重构阶段，不需要兼容历史版本、旧 API、旧协议快照、旧配置、旧数据库或已有本地数据。可以进行破坏性变更，并优先选择更清晰、最终形态更合理的设计。
- 发生破坏性变更时，直接同步更新所有调用方、schema、测试、fixture 和文档，并删除被替代的代码。不要增加兼容层、双写、旧字段 fallback、废弃别名或一次性数据迁移，除非用户明确要求保留兼容性。
- 开发数据可以清空或重建。不要为了保留当前开发快照的数据而扭曲目标数据模型。
- “无需兼容历史实现”不等于可以跳过重构目标架构本身要求的能力。例如 KAP 的版本协商仍是需要遵守的产品设计的一部分，但不需要兼容尚未发布的开发期协议快照。

### 不确定决策必须询问

- 只要对正确方向存在实质不确定性，或者多个可行方案会在架构、数据模型、安全语义、用户体验、阶段范围或长期维护成本上产生不同结果，就必须暂停实现并询问用户，不得擅自替用户选择。
- 提问前先完成必要的代码与文档调查。询问时必须说明：需要决定的问题、已知约束、每个可行方案的优势、每个方案的潜在问题与成本、影响范围，以及推荐方案和推荐理由。
- 不要只抛出一个没有上下文的开放问题，也不要把推荐方案包装成唯一选择。用户作出决定后再继续实现，并在需要时把决定写入 ADR、架构文档或重构计划。
- 能由现有文档、ADR、测试或代码边界明确推出的实现细节不属于不确定决策，应直接按既有约束执行，如果当前的约束与目标功能有冲突或者需要能力补全，又或者是违背了`正确实现优先`的原则，则应先询问用户再继续实现。

### 错误处理与失败策略

- 默认采用 fail-fast。输入、状态、协议、配置或依赖不符合预期时，应立即返回明确错误，不得用默认值、空结果或替代路径伪装成功。
- 禁止静默吞掉异常，包括空 `catch`、只打印日志后继续执行、把异常转换为 `null`、`undefined`、空数组、空对象或虚假的成功结果。
- 禁止为了“提高健壮性”加入未经产品设计确认的 fallback。Fallback 会改变可观察行为，必须被视为产品或架构决策；只有需求、协议或 ADR 明确定义时才能添加。
- 不要捕获无法在当前层真正处理的异常。不能恢复时让异常继续向上传播，由明确的系统边界统一转换为用户可见错误、RPC 错误或进程失败。
- `try/catch` 仅允许用于以下情况：
  1. 当前层能够执行明确且经过设计的恢复操作。
  2. 将底层异常转换为该层公开契约中的结构化错误，同时保留原始 `cause`。
  3. 补充对定位问题有价值的上下文后重新抛出。
  4. 执行资源清理；仅清理时优先使用 `finally`。
  5. 在进程、RPC、事件循环或 UI Error Boundary 等系统边界阻止整个宿主崩溃，并明确报告失败。
- 捕获异常后不得继续执行依赖该操作成功的后续逻辑。恢复操作必须能够证明状态仍然有效，否则应终止当前操作。
- 解析配置、数据库记录、协议帧和持久化状态失败时，默认暴露数据错误，不得自动忽略字段、重置数据或回退到默认配置，除非相应规范明确要求。
- 日志不是错误处理。`catch` 中记录日志后仍须重新抛出、返回明确失败，或执行已经定义的恢复方案。
- 错误信息必须包含操作、对象和失败原因等必要上下文，但不得泄露 API Key、凭据或其他敏感数据。
- 新增异常捕获或 fallback 时，必须通过测试覆盖正常路径、失败路径和恢复路径，并说明为什么当前层能够安全恢复。
- 修复 Bug 时，如果发现原有 fallback 掩盖了根因，应删除该 fallback，让错误在正确边界显式暴露，并补充回归测试。

> 不是要求所有错误都让 Electron 整体崩溃。正确做法是让错误传播到合适的边界，例如 RPC 返回结构化错误、UI 显示失败状态、后台任务标记为失败；关键是“明确失败”，而不是“假装成功”。

### 架构与安全约束

- Renderer 保持受限环境：`contextIsolation: true`、`sandbox: true`、`nodeIntegration: false`；不得暴露通用 `ipcRenderer`、Node.js API、文件系统能力或明文凭据。
- 当前架构中的项目访问和执行必须经过 Core；完成切换后必须经过 Agent Server。不要从 Renderer 或 Electron Main 新增绕过执行边界的捷径。
- 目标架构中的路径均为 Server 侧路径。客户端不得使用本机路径规则解释它，也不得用 Electron 原生目录选择器代替远程 `fs.browse`。
- 工具、Shell、路径授权和审批属于安全边界。新增工具或改变工具行为时，必须覆盖真实路径解析、 symlink、项目内外授权、锁、超时、取消和输出上限等相关语义。
- Agent 状态与设备偏好的归属按 ADR 0002 判断。API Key 不得进入 Renderer，也不得写入日志、测试 fixture 或未加密的持久化字段。
- 数据库结构变更必须同步更新当前实现采用的 schema、数据访问代码和相关测试。当前开发阶段不保留历史数据兼容性，可以重写尚未发布的迁移或直接重建开发数据库；不要为旧数据增加临时兼容分支。
  进入 Kotlin 持久化阶段后，按当期 SQLDelight schema 生成一套可审阅的最终迁移基线。

### UI 与协议开发约束

涉及界面、样式、动画、交互原语、主题、Inspector 卡片或 UI 插件时，开始实现前必须完整阅读[`docs/design-system.md`](docs/design-system.md)。新的通用视觉能力先进入 `packages/design-system`，
业务代码再消费对应的 token 和原语。

修改 KAP 时必须按以下顺序工作：

1. 更新 `docs/protocol/kap-v1.md` 中的线协议和语义。
2. 更新 `agent/protocol` 的 Kotlin `@Serializable` 真源。
3. 更新 `packages/protocol` 的 TypeScript + Zod 镜像。
4. 更新 `conformance/kap-v1-cases.json`，覆盖接受、拒绝和语义不变量。
5. 运行两侧一致性测试。

禁止把 Koaks 的框架类型泄漏到 KAP、客户端或 Renderer。客户端按 Server capability 决定功能是否可用，
不得根据版本号猜测，也不得在组件中内联一套协议结构。

### 开发环境与常用命令

- Node.js 22+
- pnpm 10+
- macOS 是当前首要支持平台
- Java/Gradle 任务通过仓库内的 `agent/gradlew` 运行

```bash
pnpm install
pnpm dev
KOWORK_FAKE_AGENT=1 pnpm dev
pnpm typecheck
pnpm lint
pnpm test
pnpm build
pnpm test:e2e
```

`KOWORK_FAKE_AGENT=1` 使用内置流式假 Agent。`pnpm test:e2e` 会先构建，再运行 Playwright Electron
全链路测试。平台打包命令见 `package.json` 和 `README.md`。

Kotlin/协议常用验证：

```bash
pnpm exec vitest run tests/unit/protocol-conformance.test.ts
./agent/gradlew -p agent :protocol:jvmTest
```

### 验证与完成标准

- 根据改动范围先运行最小相关测试，再运行受影响层级的完整检查。
- TypeScript 或 React 改动至少运行 `pnpm typecheck` 和相关 Vitest；共享包、构建配置或跨层改动还要运行 `pnpm build`。
- UI 行为改动补充 unit/e2e 断言；涉及 Electron、Preload、Core 或进程恢复的用户流程运行`pnpm test:e2e`。
- KAP 改动必须同时通过 TS 与 Kotlin conformance 测试。Kotlin 业务模块运行对应 Gradle 测试；阶段验收按重构计划运行完整 Gradle 构建。
- 运行 `pnpm lint` 时应与仓库既有基线区分，不得新增错误。不要为了局部改动执行会重写全仓的`pnpm format`；只格式化本次涉及的文件。
- 最终说明只列出实际执行过的检查；未执行、因环境受限无法执行或仍然失败的检查必须明确说明。

### 通用开发约定

- 文档和代码注释使用中文，代码标识符使用英文；沿用现有 TypeScript、React 和 Kotlin 风格。
- 优先做范围明确、可验证的修改。不要夹带无关重构，也不要覆盖工作区中与任务无关的已有改动。
- 先搜索现有抽象、schema、token 和测试，再新增能力；不要复制形成第二个真源。
- 只有依赖发生变化时才更新 `pnpm-lock.yaml`。不要手工编辑锁文件或仓库内的 Koaks `.tgz` 制品。
- `out/`、`dist/`、根 `build/`、`agent/**/build/`、`.gradle/`、`.kotlin/`、测试报告和缓存均为生成产物，不作为源码直接编辑或提交。
- 每做完一个独立的功能或者是修改 bug 之后，先运行相关测试和 lint，再提交。不要把多个独立功能的修改混在一个提交中。如果你在开发新功能的过程中，用户让你修复了**与本次新功能不相关**的bug或者是其他的修改，需要提示用户，是否需要把这些修改放在一个提交中，还是分开提交。
